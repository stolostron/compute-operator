# Copyright Red Hat
apiVersion: singapore.open-cluster-management.io/v1alpha1
kind: HubConfig
metadata:
  name: hub
  namespace: compute-config
spec:
  kubeconfigSecretRef:
    name: mce-kubeconfig-secret
  maxManagedCluster: 1
