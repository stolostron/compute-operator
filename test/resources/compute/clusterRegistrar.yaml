# Copyright Red Hat
apiVersion: singapore.open-cluster-management.io/v1alpha1
kind: ClusterRegistrar
metadata:
  name: {{ .Name }}
spec:
  computeService:
    computeKubeconfigSecretRef:
      name: {{ .KcpKubeconfigSecret }}
